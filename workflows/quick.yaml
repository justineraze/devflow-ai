name: quick
description: Minimal workflow — implement and gate, no planning or review

phases:
  - name: implementing
    agent: developer
    description: Write the code directly
    timeout: 900

  - name: gate
    agent: tester
    description: Run quality gate
    timeout: 300
